{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --app-dir . --reload --port 8000",
    "build": "python -m compileall .",
    "lint": "python -m compileall .",
    "type-check": "python -m compileall ."
  }
}