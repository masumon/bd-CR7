{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn main:app --app-dir . --reload --port 8000",
    "build": "python3 -m compileall .",
    "lint": "python3 -m compileall .",
    "type-check": "python3 -m compileall ."
  }
}