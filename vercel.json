{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "corepack enable; corepack prepare pnpm@10.17.1 --activate; if [ -f pnpm-lock.yaml ]; then NODE_OPTIONS=--no-deprecation pnpm install --frozen-lockfile; elif [ -f ../../pnpm-lock.yaml ]; then cd ../..; NODE_OPTIONS=--no-deprecation pnpm install --frozen-lockfile; fi",
  "buildCommand": "if [ -d apps/web-pwa ]; then cd apps/web-pwa; elif [ -d ../web-pwa ]; then cd ../web-pwa; fi; NODE_OPTIONS=--no-deprecation pnpm build",
  "devCommand": "if [ -d apps/web-pwa ]; then cd apps/web-pwa; elif [ -d ../web-pwa ]; then cd ../web-pwa; fi; NODE_OPTIONS=--no-deprecation pnpm dev",
  "functions": {
    "api/main.py": {
      "runtime": "python3.11"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/main.py"
    }
  ]
}
